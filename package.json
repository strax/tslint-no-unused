{
  "name": "tslint-no-unused-locals",
  "version": "0.1.0",
  "description": "no-unused-locals rule for TS 2.9+",
  "main": "tslint-no-unused-locals.json",
  "author": "Sami Kukkonen <sami@samikukkonen.fi>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run -s clean && tsc",
    "test": "npm run -s build && tslint -r ./dist --test test/rules/*/*",
    "prepublishOnly": "npm run -s build"
  },
  "peerDependencies": {
    "tslint": "^5.10.0",
    "typescript": ">= 2.9.1"
  },
  "devDependencies": {
    "@types/node": "^10.3.3",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"
  }
}
